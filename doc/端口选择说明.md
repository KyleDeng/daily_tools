# 端口选择说明 📡

关于服务器端口的选择和使用规范。

---

## ❓ 为什么默认使用 3000 端口？

### 历史原因

**3000 端口是 Node.js 开发中的约定俗成：**

1. **Express.js 默认**：早期 Express 框架的示例代码默认使用 3000
2. **避开系统端口**：3000 > 1024，普通用户可以使用，不需要 root 权限
3. **社区习惯**：大量教程、示例都使用 3000，形成了习惯
4. **易于记忆**：3000、3001、3002... 方便开发时区分多个服务

**并不是强制规定，只是约定俗成的习惯！**

---

## 📋 端口分类和特殊含义

### 端口范围划分

端口号范围：**0 - 65535**（共 65536 个）

| 端口范围 | 名称 | 说明 | 使用权限 |
|---------|------|------|---------|
| **0 - 1023** | 系统端口/知名端口 | 保留给系统服务 | 需要 root/管理员权限 |
| **1024 - 49151** | 注册端口 | 分配给特定应用 | 普通用户可用 |
| **49152 - 65535** | 动态/私有端口 | 临时端口 | 普通用户可用 |

---

## 🔢 常见端口及其含义

### 系统端口（0-1023）

| 端口 | 协议 | 服务 | 说明 |
|------|------|------|------|
| **20** | FTP | FTP 数据传输 | 文件传输（数据通道） |
| **21** | FTP | FTP 控制 | 文件传输（控制通道） |
| **22** | SSH | SSH/SFTP | 安全远程登录 ⭐ 重要 |
| **23** | Telnet | Telnet | 远程登录（不安全，已淘汰） |
| **25** | SMTP | 邮件发送 | 发送电子邮件 |
| **53** | DNS | 域名解析 | DNS 服务 |
| **80** | HTTP | Web 服务 | HTTP 网站 ⭐ 重要 |
| **110** | POP3 | 邮件接收 | 接收电子邮件 |
| **143** | IMAP | 邮件接收 | IMAP 邮件服务 |
| **443** | HTTPS | 安全 Web 服务 | HTTPS 网站 ⭐ 重要 |
| **465** | SMTPS | 安全邮件发送 | SSL 加密的 SMTP |
| **993** | IMAPS | 安全邮件接收 | SSL 加密的 IMAP |
| **995** | POP3S | 安全邮件接收 | SSL 加密的 POP3 |

**⚠️ 不要在应用中直接使用这些端口（除非你确实在提供这些服务）**

---

### 注册端口（1024-49151）

常见的应用端口：

| 端口 | 服务/应用 | 说明 |
|------|-----------|------|
| **1433** | MS SQL Server | 微软 SQL Server 数据库 |
| **3000** | Node.js 开发 | Node.js 应用的约定端口 ⭐ |
| **3001** | Node.js 开发 | 第二个 Node.js 应用 |
| **3306** | MySQL | MySQL 数据库 ⭐ |
| **5000** | Flask/Python | Python Web 框架默认端口 |
| **5432** | PostgreSQL | PostgreSQL 数据库 |
| **6379** | Redis | Redis 缓存数据库 |
| **8000** | Django | Django 框架默认端口 |
| **8080** | 备用 HTTP | 常用作代理或备用 Web 端口 ⭐ |
| **8443** | 备用 HTTPS | 备用 HTTPS 端口 |
| **9000** | PHP-FPM | PHP FastCGI 进程管理器 |
| **27017** | MongoDB | MongoDB 数据库 |

---

## 🎯 端口选择规则

### ✅ 可以任意选择吗？

**理论上：** 在 1024-65535 范围内可以任意选择

**实际上：** 需要遵循一些规则和最佳实践

---

## 📝 端口选择最佳实践

### 1️⃣ **避开已知端口**

❌ **不要使用：**
```
22    # SSH，可能冲突
80    # HTTP，通常被 Nginx/Apache 占用
443   # HTTPS，通常被 Nginx/Apache 占用
3306  # MySQL
5432  # PostgreSQL
6379  # Redis
27017 # MongoDB
```

### 2️⃣ **推荐的端口范围**

✅ **适合 Web 应用的端口：**
```
3000-3999   # Node.js 应用（约定俗成）
4000-4999   # 其他 Web 应用
5000-5999   # Python/Flask 应用
8000-8999   # 备用 Web 端口
9000-9999   # 其他服务
```

### 3️⃣ **多个服务时的规划**

**示例：同一服务器运行多个应用**

```bash
# 应用端口规划
3000  # daily_tools（主应用）
3001  # blog（个人博客）
3002  # api-server（API 服务）
3003  # admin-panel（管理后台）
```

**或按类型分段：**

```bash
# 前端应用
3000-3099

# 后端 API
4000-4099

# 数据库相关
5000-5099

# 其他服务
6000-6099
```

### 4️⃣ **检查端口是否被占用**

**部署前必做：**

```bash
# 检查端口占用
sudo netstat -tlnp | grep :3000

# 或
sudo lsof -i :3000

# 如果有输出，说明端口已被占用
```

### 5️⃣ **使用 Nginx 反向代理**

**最佳实践：应用使用高端口，Nginx 监听 80/443**

```
用户访问
  ↓
https://example.com:443 (Nginx)
  ↓
http://localhost:3000 (你的应用)
```

**优点：**
- ✅ 应用不需要 root 权限
- ✅ 用户访问标准端口（80/443）
- ✅ 可以轻松切换应用端口
- ✅ 支持多个应用共享 80/443

---

## 🔒 安全建议

### 1️⃣ **不要直接暴露应用端口**

❌ **不推荐：**
```
用户 → http://服务器IP:3000 → 应用
```

✅ **推荐：**
```
用户 → https://域名:443 → Nginx → http://localhost:3000 → 应用
```

### 2️⃣ **防火墙只开放必要端口**

```bash
# 只开放 Nginx 端口
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS

# 不开放应用端口（3000）
# 应用只监听 localhost，不对外暴露
```

### 3️⃣ **应用监听 localhost 而非 0.0.0.0**

**安全配置：**
```javascript
// Node.js 应用
app.listen(3000, 'localhost', () => {
  console.log('Server running on localhost:3000');
});
```

**不安全配置：**
```javascript
// ❌ 监听所有网卡，可以从外部直接访问
app.listen(3000, '0.0.0.0', () => {
  console.log('Server running on 0.0.0.0:3000');
});
```

---

## 💡 实际场景示例

### 场景1：单个应用

```
# 应用配置
应用端口: 3000 (localhost)

# Nginx 配置
server {
    listen 80;
    server_name example.com;
    
    location / {
        proxy_pass http://localhost:3000;
    }
}

# 防火墙
开放: 22, 80, 443
不开放: 3000（因为只监听 localhost）
```

### 场景2：多个应用

```
# 应用配置
daily_tools:  localhost:3000
blog:         localhost:3001
api:          localhost:4000

# Nginx 配置
server {
    listen 80;
    server_name tools.example.com;
    location / {
        proxy_pass http://localhost:3000;  # daily_tools
    }
}

server {
    listen 80;
    server_name blog.example.com;
    location / {
        proxy_pass http://localhost:3001;  # blog
    }
}

server {
    listen 80;
    server_name api.example.com;
    location / {
        proxy_pass http://localhost:4000;  # api
    }
}

# 防火墙
开放: 22, 80, 443
不开放: 3000, 3001, 4000（都只监听 localhost）
```

---

## 🎯 项目端口选择建议

### 对于 daily_tools 项目

**当前配置（3000）的理由：**

1. ✅ **符合 Node.js 社区习惯**
2. ✅ **避开系统端口（0-1023）**
3. ✅ **不需要 root 权限**
4. ✅ **易于记忆和文档说明**
5. ✅ **与大多数教程保持一致**

**可以改成其他端口吗？**

当然可以！只需修改：

```bash
# Docker Compose
ports:
  - "8080:3000"  # 外部 8080，内部 3000

# PM2 直接部署
pm2 start serve --name daily-tools -- -s dist -l 8080

# Nginx 代理
proxy_pass http://localhost:8080;
```

**但建议保持 3000，因为：**
- 文档统一
- 社区习惯
- 没有冲突

---

## 🔍 快速检查命令

### 查看端口占用

```bash
# 查看所有监听端口
sudo netstat -tlnp

# 查看特定端口
sudo netapp -tlnp | grep :3000
sudo lsof -i :3000

# 查看进程监听的端口
sudo lsof -i -P -n | grep LISTEN
```

### 测试端口连接

```bash
# 测试本地端口
curl http://localhost:3000

# 测试远程端口
telnet 服务器IP 3000
# 或
nc -zv 服务器IP 3000
```

---

## 📚 总结

### ✅ 端口选择原则

1. **避开系统端口**（0-1023）
2. **避开已知应用端口**（MySQL 3306、Redis 6379 等）
3. **在 1024-65535 范围内选择**
4. **遵循社区习惯**（Node.js 用 3000）
5. **检查是否被占用**
6. **使用 Nginx 反向代理**

### ✅ 为什么我们用 3000

- ✅ Node.js 社区约定
- ✅ 大部分教程和示例使用
- ✅ 避开系统端口
- ✅ 易于记忆
- ✅ **没有强制规定，只是习惯**

### ✅ 其他常用端口

- **3000**: Node.js 应用（约定）
- **8080**: 备用 Web 服务
- **5000**: Python/Flask 应用
- **8000**: Django 应用
- **4200**: Angular 开发服务器
- **5173**: Vite 开发服务器
- **8000-9000**: 通用备用端口

**记住：端口只是一个数字，选择合适的、不冲突的就好！** 🎯

